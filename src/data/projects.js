import { Database, Navigation, Coffee, Search, Cpu } from 'lucide-react'

const projects = [
  {
    slug: 'kv-store',
    title: 'KV Store',
    shortDesc: 'Redis-inspired key-value store in C++ with thread pool, LRU eviction, and append-only log for crash recovery. Benchmarked at 15–17k cmd/s over TCP.',
    overview: 'I built flashkv to get hands-on experience with the systems-level problems that databases like Redis solve — things that are easy to take for granted when you\'re just calling a library. The goal was to implement a working key-value store from scratch in C++17 with no external dependencies: just POSIX sockets, the standard library, and pthreads. It supports GET, SET, DEL, and PING over a plain-text TCP protocol, survives restarts via an append-only log, and handles concurrent clients through a thread pool. Building it forced me to think carefully about cache consistency, crash recovery semantics, and safe concurrent access. These are important considerations that are usually just hidden behind abstractions.',
    technical: 'Thread pool. The server spawns a fixed pool of 4 worker threads at startup. Each worker blocks on a condition variable, waiting for tasks to appear in a shared queue. The main accept loop runs on the main thread — it calls `accept()`, gets a client file descriptor, and immediately queues a lambda capturing that fd into the task queue. A worker wakes up, handles the entire client session (reading, parsing, responding), then closes the connection and goes back to waiting. Shutdown sets a stop flag and broadcasts on the condition variable so all workers drain the queue and exit cleanly.\n\nLRU eviction. The store uses the classic doubly-linked list + hash map combination. The `std::list` maintains entries in recency order — the front is the most recently used key, the back is the least recently used. The `std::unordered_map` maps each key to its iterator in the list, giving O(1) lookup. On every GET or SET, the accessed entry is spliced to the front of the list in O(1) using `std::list::splice`. When a SET causes the map to exceed the key cap (default 1000), the entry at the back of the list is evicted — it\'s erased from both the map and the list. The whole store is protected by a `std::shared_mutex`.\n\nAOF persistence. Every write command (SET, DEL) is appended as a plain-text line to `kvstore.aof` and flushed to disk immediately before the response is sent. On startup, the server calls `replay()`, which reads the file line by line and re-executes each command against the store to rebuild state. No custom binary format is needed. The tradeoff is that the log grows unboundedly and replay time scales linearly with history (26,000 entries on a recent run), but it keeps the implementation simple and correct: if the process crashes mid-write, at worst the last command is missing, and the rest of the log is intact.\n\nTCP server. The server creates a standard POSIX TCP socket, binds to the configured port, and loops on `accept()`. Each accepted file descriptor is handed off to the thread pool immediately, so the accept loop is never blocked by slow clients. Per-client handling reads into a 4 KB buffer and appends to an accumulator string, splitting on newlines to handle partial reads correctly. Each complete line is tokenized by whitespace and dispatched to GET/SET/DEL/PING handlers. Responses use a +/- prefix convention — `+OK` or `+<value>` for success, `-ERR <reason>` for errors. This was loosely inspired by Redis\'s RESP protocol but simpler.',
    stack: 'C++ · Sockets · Pthreads',
    tags: ['Systems', 'C++'],
    github: 'https://github.com/athp18/flashkv',
    live: null,
    images: ['/images/flashkv.png'],
    icon: Database,
  },
  {
    slug: 'baymaps',
    title: 'BayMaps',
    shortDesc: 'Traffic-aware routing app for the SF Bay Area using real Caltrans sensor data and XGBoost congestion prediction.',
    overview: 'I built this to understand how routing apps like Google Maps actually work under the hood. The Bay Area made sense because of its scope. The graph is of a manageable size, and Caltrans publishes free 5-minute sensor data from thousands of roadside stations via PeMS. I wanted to replicate the core loop: ingest real traffic data, predict congestion on roads sensors don\'t cover, and route over a full road graph. (Of course this application is limited by the lack of real-time data availability.)',
    technical: 'The backend loads a 95k-node, 235k-edge [OSMnx](https://osmnx.readthedocs.io/en/stable/) road graph and runs a custom Dijkstra with three-tier edge weights: real PeMS sensor speeds for ~1,400 directly covered edges, XGBoost-predicted congestion multipliers for everything else, and OSM speed limits as a fallback.\n\nThe PeMS pipeline snaps sensor stations to road segments by a [KDTree](https://docs.scipy.org/doc/scipy/reference/generated/scipy.spatial.KDTree.html) and aggregates speeds into a 168-hour-of-week parquet lookup. Six XGBoost models are trained, one per time bucket (AM peak, midday, PM peak, night, weekend day, weekend night). Each edge is featurized with road type, speed, length, node degree, and distance/congestion-ratio features from the 5 nearest sensors. I tried a GNN first but R2 was negative across all buckets, the main reason being that 0.6% labeled edge coverage is too sparse for message passing to work.\n\nRoute results are cached in Redis by (origin, destination, hour-of-week) with a 5-minute TTL. The frontend is React + Leaflet with Bay Area-bounded autocomplete, turn-by-turn directions from OSM street names and bearing math, and an ETA confidence range based on per-bucket model R2 (ie lower R2 tends to have a higher confidence range, and vice versa).',
    stack: 'React · TypeScript · FastAPI · XGBoost · Redis · Docker',
    tags: ['Web', 'AI/ML', 'Full Stack'],
    github: 'https://github.com/athp18/BayMaps',
    live: null,
    images: ['/images/BayAreaMaps_1.png', '/images/BayAreaMaps_2.png'],
    icon: Navigation,
  },
  {
    slug: 'brewbuddy',
    title: 'BrewBuddy',
    shortDesc: 'Mobile app for discovering local coffee shops with an AI-integrated recommendation engine using user preferences, ratings, and real-time location.',
    overview: 'Coffee shops have always been one of the ways that I explore new places — whenever I\'m in a new neighborhood, finding a good café is one of the first thing I like to do. The problem is that Yelp and Google Maps tell you what\'s popular, not what\'s right for you. A 4.4-star spot that\'s known for pour-overs isn\'t useful if you\'re a latte person who needs good WiFi and a quiet corner to work. BrewBuddy (tries) to handle that frustration: I wanted something that actually knew my taste and could surface the right shop the moment I landed somewhere new.\n\nThe recommendation engine is the main thing that I tinkered with and that interested me. It builds a taste profile from your review history and uses it to rank every nearby shop in real time, so the first result is the one that actually fits how you drink coffee and how you like to spend time in a café.',
    technical: 'Stack: React (Vite) frontend deployed on Vercel, Node.js/Express API on Render, MongoDB Atlas for persistence, Google Places API for shop data, and the Anthropic Claude API for AI-generated feed summaries.\n\nRecommendation engine: Every nearby shop gets a `brewScore`, which is a weighted blend of five signals: Google rating (30%), distance (25%), drink/vibe tag match (20%), price preference (15%), and past review sentiment (10%). The tag match compares a user\'s preference tags against tags that BrewBuddy reviewers have applied to that specific shop. For shops without any BrewBuddy reviews yet, the engine falls back to signals parsed from Google review text using a keyword map. These are in MongoDB so subsequent loads don\'t need extra API calls.\n\nTaste profile: Every review recomputes a dynamic taste profile: normalized drink and vibe weights, a revealed average price level, and a review count. Higher-rated reviews carry more weight (for example, a 5-star review counts 2.5x more than a 2-star one). This profile drives both the `brewScore` ranking and the "Because you like lattes" / "Because you like quiet spots" row groupings in the feed.\n\nPhoto proxy: Rather than exposing Google Places photo URLs (with the API key embedded) to the client, all images are proxied through `/api/photos/:reference`. The server follows the Google redirect once, caches the resolved CDN URL in MongoDB with a 7-day TTL, and streams subsequent requests with `Cache-Control: public, max-age=604800`. This handles two main cases: (1) where the Google Places API is not returning the expected behavior, and (2) when we are getting rate limited. This means that each unique photo only hits Google once, and each café always has a photo.\n\nAuth: JWT-based with Google OAuth as a zero-friction alternative. The OAuth flow is implemented without Passport.js — a manual authorization code exchange against Google\'s token endpoint, with the resulting JWT passed back to the client via redirect.',
    stack: 'React Native · MongoDB · Node.js',
    tags: ['Mobile', 'Full Stack', 'Web'],
    github: 'https://github.com/athp18/BrewBuddy',
    live: 'https://brew-buddy-client.vercel.app/',
    images: ['/images/BrewBuddy_img1.jpg', '/images/BrewBuddy_img2.png'],
    icon: Coffee,
  },
  {
    slug: 'competitive-intelligence',
    title: 'Competitive Intelligence',
    shortDesc: 'A signal ingestion and multi-agent query platform that continuously crawls job boards, news, and developer platforms, extracts structured intelligence via LLM, and lets you query the knowledge base in plain English.',
    overview: 'Most competitive intelligence workflows fall into one of two failure modes: you\'re either querying an LLM that confidently answers from training data that\'s months or years stale, or you\'re paying for an enterprise tool (Crayon, Klue, Similarweb) that aggregates surface-level signals without letting you ask freeform questions against the underlying data. Neither gives you a continuously updated, queryable knowledge base you actually control.\n\nThis engine fills that gap. It continuously ingests real signals — ie job postings, GitHub commits, HN discussions, news articles, arXiv papers — extracts structured intelligence from them via LLM, and stores everything in a vector + relational database you can query in plain English. The answers are strictly grounded in what was actually observed, not model priors. As signal volume grows, the system becomes meaningfully more useful: hiring patterns emerge, product direction shifts become visible, and cross-company comparisons become possible.\n\nTarget users are product managers tracking competitor launches, investors monitoring portfolio companies and sector trends, and strategy or research teams who need ongoing awareness of a technical topic or organization.',
    technical: 'Celery workers run on two queues — `crawl` for source-specific scrapers (HN, NewsAPI, GitHub, arXiv, Google News, career pages) and `llm` for extraction tasks. Celery Beat schedules incremental crawls per source on configurable cadences (news every 6h, GitHub daily, etc.). Manual trigger runs bypass the schedule and run exhaustively, fetching full history rather than just recent items.\n\nAfter each crawl, an extraction task batches raw items and calls Claude Haiku with a structured prompt to classify each item into a typed signal (hiring, product, research, funding, mention) with a one-sentence summary, relevance score, and date. Each signal summary is then embedded via Voyage AI (`voyage-3`, 1024 dims) in a single batched API call and written to PostgreSQL with `pgvector`. Deduplication runs by content hash before any DB write.\n\nQueries go through a four-agent pipeline. A Sonnet orchestrator resolves the target name to a database ID and decides which retrieval agents to invoke. A Haiku semantic search agent reformulates the query for vector retrieval and runs cosine similarity search against `pgvector`. A Haiku SQL agent extracts structured filters (signal type, date range, relevance tier) and runs a direct relational lookup. A Sonnet summary agent receives the deduplicated union of both result sets and synthesizes a grounded answer — agents are explicitly instructed not to supplement with training knowledge. The Next.js frontend communicates with a FastAPI backend over a thin REST API, with `SQLAlchemy` + `asyncpg` handling all DB I/O.',
    stack: 'FastAPI · PostgreSQL/pgvector · Celery · Next.js · Claude API · Voyage AI',
    tags: ['AI/ML', 'Full Stack'],
    github: 'https://github.com/athp18/competitive-intelligence',
    live: null,
    images: ['/images/CompIntelligence_1.png', '/images/CompIntelligence_2.png'],
    icon: Search,
  },
  {
    slug: 'llm-bench',
    title: 'Distributed LLM Benchmark',
    shortDesc: 'A fine-tuning benchmark harness that sweeps training configs across single-GPU and multi-GPU FSDP setups, measuring throughput, peak memory, and step latency.',
    overview: 'Most standard training benchmarks either measure inference throughput or report a single aggregate number per run. I wanted something that captures the full picture of distributed fine-tuning specifically — how sharding strategy, dtype, and gradient checkpointing interact across memory and throughput simultaneously, with per-step resolution rather than just a final average.\n\nTools like torchrun\'s built-in logging are too coarse, and general profilers (nsight, torch.profiler) produce more overhead and complexity than needed for ablation sweeps. The goal was a lightweight harness that could run a full configuration matrix, write structured JSON per run, and make the tradeoffs immediately visible.',
    technical: 'The sweep logic lives in `sweep.py` and is built around a base config + overrides pattern: you define a `BenchmarkConfig` and pass a list of dot-notation override dicts (e.g. `"fsdp.sharding_strategy": "FULL_SHARD"`), which `make_sweep_configs` deep-copies and applies before handing off to the trainer. The three preset sweeps — `single_vs_fsdp`, `grad_checkpoint`, and `full_ablation` — are thin wrappers over this.\n\nFSDP wrapping happens in `trainer.py`. The model is loaded onto CPU first to avoid rank-0 OOM during sharding, then wrapped with `FullyShardedDataParallel` using `transformer_auto_wrap_policy` — which auto-detects the decoder layer class (`LlamaDecoderLayer`, `GPT2Block`, etc.) via a name scan so the policy doesn\'t need to be hardcoded per architecture. Sharding strategy, mixed precision policy, and CPU offload are all pulled from the config.\n\nPer-step metrics are collected by `MetricsCollector` in `metrics.py`. It calls `torch.cuda.synchronize()` before starting and stopping the step timer so the elapsed time reflects actual GPU work rather than kernel launch latency. After each optimizer step it reads `torch.cuda.memory_allocated()` and `memory_reserved()`, computes tokens/sec from tokens_per_step / elapsed, and appends a `StepMetrics` dataclass to the result. If `pynvml` is installed, GPU utilization percentage is captured too. At the end of each run, `compute_summary()` derives mean throughput, peak memory, and total time from the step list, and the full result (including all 50 per-step records) is written to `results/<run_id>.json`.\n\nThe dashboard is a single static HTML file. `inject_results.sh` reads every JSON file in `results/`, merges them into one array, and substitutes it into the `const RUNS = []` placeholder in `dashboard.html` via a Python one-liner regex replace. Chart.js renders four views from that array: a dual-axis bar chart (throughput + peak memory) with filter chips, an efficiency frontier scatter plot (throughput vs. memory, colored by dtype), a grad checkpoint impact chart that pairs matched runs by dtype and sharding strategy, and per-step line charts for throughput, loss, and GPU memory with individually toggleable runs.',
    stack: 'PyTorch · FSDP · TinyLlama · torchrun · Chart.js',
    tags: ['AI/ML', 'Systems'],
    github: 'https://github.com/athp18/llm_bench',
    live: null,
    images: ['/images/LLMBench_1.png', '/images/LLMBench_2.png'],
    icon: Cpu,
  },
]

export default projects
